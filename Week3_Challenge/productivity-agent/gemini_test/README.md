Run with `py test.py` from inside this `gemini_test/` folder (it reads the API key from the `.env` file in the project root automatically).

Never commit a real API key to this folder or anywhere else in the repo.
